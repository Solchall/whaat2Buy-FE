const SkeletonBox = 'w-[180px] h-[220px] border-2 border-zinc-800 rounded-md';

const SkeletonShade = 'flex animate-pulse flex-row items-center h-full justify-center';

const SkeletonContent = 'w-full h-full bg-zinc-800';

const S = {
  SkeletonBox,
  SkeletonShade,
  SkeletonContent,
};

export default S;
