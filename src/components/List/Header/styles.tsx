const HeaderContainer = 'flex flex-row justify-between items-center my-1';
const Button =
  'cursor-pointer p-[8px] flex flex-row bg-zinc-400/40 rounded-3xl border border-white/70 hover:bg-purple-400/50';

const TitleWrapper = 'text-white text-2xl sm:text-3xl lg:text-4xl lg:leading-normal font-extrabold';

const Title = 'text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-600';

const Icon = {
  fontSize: '18px',
  color: '#fff',
};

const S = {
  HeaderContainer,
  Button,
  Icon,
  Title,
  TitleWrapper,
};

export default S;
