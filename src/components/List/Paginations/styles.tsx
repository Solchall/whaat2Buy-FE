const PaginationContainer = 'pagination w-[60px] py-4 flex flex-row justify-between';
const Button = 'text-white';
const ActiveButton = 'bg-purple-400 rounded-xl';
const InActiveButton = 'bg-pink-100 rounded-xl';

const S = {
  PaginationContainer,
  Button,
  ActiveButton,
  InActiveButton,
};

export default S;
